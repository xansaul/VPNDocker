services:
  hub:
    build: 
      context: .
      dockerfile: Dockerfile.hub
    ports:
      - "7878:7878"

  cliente_local:
    build:
      context: .
      dockerfile: Dockerfile.client
    environment:
      - SERVER_ADDR=hub:7878
    depends_on:
      - hub
